{
  "name": "chuck-norris-api-test",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "npm run compile && node ./lib/src/main.js",
    "compile": "./node_modules/.bin/babel ./src/*.js --out-dir lib",
    "test": "npm run compile && mocha *./test/*test.js"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.7.0",
    "chai": "^4.1.2",
    "mocha": "^5.2.0"
  },
  "dependencies": {
    "axios": "^0.18.0"
  }
}
